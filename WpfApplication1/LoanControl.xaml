<UserControl x:Class="WpfApplication1.LoanControl"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApplication1"
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300"
   >
    <Grid>
        <DataGrid  AutoGenerateColumns="False" ItemsSource="{Binding LoanItem}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="KundenNr" Binding="{Binding CustomerId}"/>
                <DataGridTextColumn Header="Name" Binding="{Binding Customer.Name}"/>
                <DataGridTextColumn Header="Ausleihen" Binding="{Binding Gadget.Name}"/>
                <DataGridTextColumn Header="Loan-Datum" Binding="{Binding PickupDate}"/>
                <DataGridTextColumn Header="Return-Datum" Binding="{Binding OverDueDate}"/>
                <DataGridCheckBoxColumn IsReadOnly="True" Header="Hat überfällige" Binding="{Binding IsOverdue,Mode=OneWay}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
